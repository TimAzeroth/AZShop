<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.azeroth.project.repository.SalesRepository">

    <resultMap id="mapUserImp" type="com.azeroth.project.domain.UserDomain">
        <result column="u_username" property="u_username"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
    </resultMap>

    <resultMap id="mapAddImp" type="com.azeroth.project.domain.AddressDomain">
        <result column="address_detail" property="address_detail"/>
        <result column="address" property="address"/>
        <result column="postcode" property="postcode"/>
    </resultMap>

    <resultMap id="mapPdImp" type="com.azeroth.project.domain.ProductDomain">
        <result column="p_id" property="id"/>
    </resultMap>

    <select id="findSales" resultMap="mapUserImp, mapAddImp, mapPdImp">
        select
            u.username "u_username",
            u.nickname "nickname",
            u.email "email",
            u.phone "phone",
            a.address_detail "address_detail",
            a.address "address",
            a.postcode "postcode",
            p.id "p_id"
        from
            address a,
            `user` u,
            product p
        WHERE
            username = #{username}
    </select>

</mapper>